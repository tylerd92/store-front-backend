import Client from '../database';

export type User = {
  id: number;
  username: string;
  password: string;
  email: string;
};

export class UserStore {
  // will contain code to get data from the database
  /*
    index
    show 
    create
    update
    delete
  */
};