import Client from '../database';

export type Order = {
  id: number;
  orderStatus: string;
  userId: number
};

export class OrderStore {
  // will contain code to get data from the database
  /*
    Methods
    index
    show 
    create
    update
    delete
  */
};